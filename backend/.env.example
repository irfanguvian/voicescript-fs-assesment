# Copy to .env and adjust as needed.
# Matches the postgres service in the repo-root docker-compose.yml.
DATABASE_URL="postgresql://courtuser:courtpass@localhost:5433/courtdb?schema=public"

# Server
PORT=3000

# Payment rules (Phase 4)
REPORTER_RATE_PER_MINUTE=2000
EDITOR_FLAT_FEE=50000
